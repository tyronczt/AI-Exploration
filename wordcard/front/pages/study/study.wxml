<!--study.wxml-->
<view class="container">
  <!-- 学习进度信息 -->
  <view class="study-info">
    <view class="info-item">
      <text class="info-label">新学词</text>
      <text class="info-value">{{newWordsCount}}/{{totalNewWords}}</text>
    </view>
    <view class="info-item">
      <text class="info-label">待复习</text>
      <text class="info-value">{{reviewWordsCount}}/{{totalReviewWords}}</text>
    </view>
  </view>

  <!-- 单词卡片 -->
  <view class="word-card" wx:if="{{currentWord}}">
    <!-- 单词 -->
    <view class="word-title">{{currentWord.word}}</view>
    
    <!-- 例句 -->
    <view class="example-sentence">
      <text>{{currentWord.exampleSentence}}</text>
    </view>
    
    <!-- 选项按钮 -->
      <view class="option-buttons">
        <view wx:for="{{currentWord.options}}" wx:key="index"
              class="option-btn {{selectedOption == index ? 'selected' : ''}}"
              bindtap="selectOption"
              data-option="{{index}}">
          <text>{{item}}</text>
        </view>
      </view>
  </view>

  <!-- 功能按钮 -->
  <view class="function-buttons" wx:if="{{currentWord}}">
    <view class="func-btn" bindtap="addToWordbook">
      <text class="func-icon">📚</text>
      <text class="func-text">加入单词本</text>
    </view>
    <view class="func-btn" bindtap="showHint">
      <text class="func-icon">💡</text>
      <text class="func-text">提示</text>
    </view>
  </view>

  <!-- 下一个按钮 -->
  <view class="next-button" wx:if="{{currentWord}}" bindtap="nextWord">
    <text class="next-text">下一个</text>
  </view>

  <!-- 加载状态 -->
  <view class="loading" wx:if="{{loading}}">
    <view class="loading-text">加载中...</view>
  </view>
  
  <!-- 空状态 -->
  <view class="empty" wx:if="{{!loading && !currentWord}}">
    <view class="empty-text">请先选择词库</view>
  </view>
</view>